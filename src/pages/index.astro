---
/**
 * HOME. All sections live in HomeSections so /variants can render the same
 * markup three times under different accent tokens.
 */
import Base from '../layouts/Base.astro';
import HomeSections from '../components/HomeSections.astro';
---

<Base title="a textured word" bare>
  <HomeSections />
</Base>
